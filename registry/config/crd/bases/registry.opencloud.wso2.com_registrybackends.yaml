---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: registrybackends.registry.opencloud.wso2.com
spec:
  group: registry.opencloud.wso2.com
  names:
    kind: RegistryBackend
    listKind: RegistryBackendList
    plural: registrybackends
    shortNames:
    - rb
    singular: registrybackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.installedVersion
      name: Version
      type: string
    - jsonPath: .status.endpoint.address
      name: Address
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RegistryBackend is the tenant Harbor cluster CR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegistryBackendSpec describes the desired state of one tenant's
              Harbor cluster.
            properties:
              cpu:
                anyOf:
                - type: integer
                - type: string
                default: "2"
                description: CPU is the Kubernetes CPU quantity to request for Harbor's
                  core+registry pods combined.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              engineConfig:
                description: EngineConfig holds Harbor-specific tuning.
                properties:
                  addons:
                    description: Addons controls optional Harbor sub-components.
                    properties:
                      trivy:
                        description: Trivy enables the Trivy vulnerability scanner.
                        properties:
                          enabled:
                            default: false
                            description: Enabled turns the addon on (true) or off (false).
                            type: boolean
                        type: object
                    type: object
                  externalURL:
                    description: ExternalURL is the URL Harbor advertises to docker
                      clients. Empty means Harbor uses in-cluster Service DNS only.
                    type: string
                  haReplicas:
                    default: 1
                    description: |-
                      HAReplicas is the replica count for the Harbor core. v0.1 supports
                      only 1; values > 1 require a PostgreSQL + Redis HA topology that is
                      not yet implemented. The CRD enforces maximum: 1 until HA is wired.
                    maximum: 1
                    minimum: 1
                    type: integer
                type: object
              harborVersion:
                default: 1.14.0
                description: HarborVersion is the Harbor Helm chart version to install.
                  Must match a tarball committed under charts/.
                minLength: 1
                type: string
              memoryGB:
                default: 4
                description: MemoryGB is the memory request in GiB for the Harbor
                  pods combined.
                minimum: 1
                type: integer
              storageClass:
                description: StorageClass is the Kubernetes StorageClass for Harbor's
                  PVCs. Empty uses the cluster default.
                type: string
              storageGB:
                default: 100
                description: StorageGB is the size of the PVC backing Harbor's registry
                  storage.
                minimum: 10
                type: integer
            type: object
          status:
            description: RegistryBackendStatus is what the operator writes back.
            properties:
              conditions:
                description: Conditions follow standard Kubernetes status conventions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: Endpoint is the in-cluster reachability of this Harbor.
                properties:
                  address:
                    description: Address is the in-cluster Service DNS name or IP.
                    type: string
                  port:
                    description: Port is the TCP port Harbor's core service listens on.
                    type: integer
                  secretRef:
                    description: SecretRef points at a Secret holding admin credentials.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - address
                - port
                type: object
              installedVersion:
                description: InstalledVersion is the Harbor chart version actually
                  installed. Differs from spec.harborVersion during an in-flight upgrade.
                type: string
              message:
                description: Message is the human-readable explanation of the current
                  phase.
                type: string
              observedGeneration:
                description: ObservedGeneration is the metadata.generation the operator
                  has reconciled against.
                format: int64
                type: integer
              phase:
                description: Phase mirrors the managed-services contract enum.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Failed
                - Terminating
                type: string
              resources:
                description: Resources tracks every child Kubernetes object for idempotency.
                items:
                  description: ResourceRef identifies one Kubernetes object owned
                    by the CR.
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    version:
                      type: string
                  required:
                  - kind
                  - name
                  - version
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
